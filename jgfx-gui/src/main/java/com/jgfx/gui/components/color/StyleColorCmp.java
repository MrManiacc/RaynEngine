package com.jgfx.gui.components.color;

import com.jgfx.gui.elements.AbstractElement;

/**
 * Used for things like strike through in text, or anything that isn't a foreground/background color
 */
public class StyleColorCmp extends AbstractColorCmp {
    public StyleColorCmp(AbstractElement parent) {
        super(parent);
    }
}
